"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import type { DashboardSnapshot } from "@/lib/domain";
import { createBrowserSupabaseClient } from "@/lib/supabase/browser";
export function DashboardClient({ initial }: { initial: DashboardSnapshot }) { const [data, setData] = useState(initial); useEffect(() => { const client = createBrowserSupabaseClient(); if (!client) return; client.auth.getSession().then(({ data: { session } }) => { if (!session) window.location.assign("/login"); }); }, []); const primary = data.cases[0]; return <main className="simple-dashboard"><header className="dash-nav"><Link href="/" className="dash-brand"><span>◈</span> Sentinel</Link><p>READ-ONLY INVESTIGATOR WORKSPACE</p><div><button onClick={() => fetch("/api/dashboard").then((response) => response.json()).then(setData)}>Refresh</button><Link href="/login">Profile</Link></div></header><section className="dash-content"><div className="dash-intro"><div><p>RISK OVERVIEW</p><h1>Evidence before action.</h1><span>Current merchant workspace · Test Mode</span></div><div className="dash-safety">✦ No customer outcome is changed here.</div></div><div className="simple-kpis"><article><span>Open rings</span><b>{data.cases.length}</b><small>Manual review queue</small></article><article><span>Estimated exposure</span><b>₹{data.cases.reduce((sum, ring) => sum + ring.exposureInr, 0).toLocaleString("en-IN")}</b><small>Across visible cases</small></article><article><span>Precision / recall</span><b>{data.metrics.precision}% / {data.metrics.recall}%</b><small>Held-out ring dataset</small></article></div><section className="focus-case"><div className="focus-copy"><p>PRIORITY CASE / {primary.id}</p><h2>Five accounts. Four independent links.</h2><ul>{primary.evidence.map((item) => <li key={item.kind}><i className={item.strength}/><span>{item.label}</span><small>{item.detail}</small></li>)}</ul><Link href={`/cases/${primary.id}`}>Inspect evidence graph →</Link></div><div className="focus-score"><span>REVIEW CONFIDENCE</span><b>{primary.confidence}</b><small>/100</small><hr/><p>₹{primary.exposureInr.toLocaleString("en-IN")} promotion exposure</p><em>Manual investigation only</em></div></section><section className="simple-queue"><div><p>QUEUE</p><h2>Cases awaiting review</h2></div>{data.cases.slice(0, 3).map((ring) => <Link className="queue-row" href={`/cases/${ring.id}`} key={ring.id}><b>{ring.id}</b><span>{ring.accountIds.length} linked accounts · {ring.couponCode}</span><strong>{ring.score}</strong><i>→</i></Link>)}</section></section></main>; }
